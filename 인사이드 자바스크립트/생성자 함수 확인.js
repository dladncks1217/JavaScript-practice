function 생성자check(name){
        this.name = name;
        console.log(this.name);
        console.log(this);
        return other;
      }
      var name = '임우찬';

      function other객체(name){
        this.name = name;

      }
      var check = new 생성자check('hi');

      var other = new other객체('hello');
      function 익명함수(){
      console.log(this.name);
    }
    익명함수();